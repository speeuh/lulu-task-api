spring:
  application:
    name: luiza-tasks-backend
  
  datasource:
    url: jdbc:postgresql://db.vpkrikfjuxhentbmdeaf.supabase.co:5432/postgres
    username: postgres
    password: PassLuluTasks
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  secret: luiza-tasks-secret-key-change-this-in-production-please
  expiration: 86400000 # 24 hours in milliseconds

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173
  
  # Default users configuration (REQUIRED)
  admin:
    username: admin
    password: admin123  # CHANGE THIS IN PRODUCTION!
    fullname: Administrador
  
  user:
    username: lulu
    password: namoradadolucas  # CHANGE THIS IN PRODUCTION!
    fullname: Lulu
  
  init:
    createdefaultusers: true  # Set to false after first deploy in production

